import { View, Text } from 'react-native';

const ExpenseTypeManagement = () => {
  return (
    <View>
      <Text>This is the screen to manage an expense types</Text>
    </View>
  );
};

export default ExpenseTypeManagement;
